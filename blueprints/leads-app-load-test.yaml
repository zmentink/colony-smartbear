spec_version: 1
kind: blueprint
metadata:
  description: >
    Application is configured to deploy with multiple instances for load tests.
    
clouds: 
  - AWS: us-west-1
  
artifacts:
  - leads: latest/leads-webapp.latest.tar.gz
  - smartbear-servicev: sf-virtual-service/leads-virt.war

inputs:
- AWS_INSTANCE_TYPE: m5.large

applications:
  - leads:
      instances: 1
      input_values:
        - AWS_INSTANCE_TYPE: $AWS_INSTANCE_TYPE
        - SALESFORCE_LOGIN_URL: http://smartbear-servicev.{COLONY}.sandbox.com:8080/leads-virt
    
  - db:      
      input_values:
        - AWS_INSTANCE_TYPE: $AWS_INSTANCE_TYPE

  - smartbear-servicev:
            
debugging:
  availability: on